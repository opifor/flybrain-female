# secrets - the wallets' private keys live here
.env
.env.*
!.env.example

# 1.1 GB of connectome downloaded from Janelia's public bucket; the README
# says exactly how to fetch it, and build_graph.py turns it into build/graph.npz
data/

# derived artefacts, recordings, scratch
build/

# her state: the paper ledger, what she learned, her day. Backed up by keepalive.ps1, never committed
state/
scratch/
__pycache__/
*.pyc
*.mp4
*.webm

# trained gains are small and reproducible, but they ARE a result - keep them
!build/gains_ui.npz

# local virtualenv
.venv/

# the relay worker: local dev state and installed tools
site/relay/.wrangler/
site/relay/node_modules/
.wrangler/
site/.wrangler/
site/web/music/
