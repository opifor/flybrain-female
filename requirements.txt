# What the fly needs. Versions are the ones it has actually been run against.
#
# After installing these, one more step:  python -m playwright install chromium
#
# Then fetch her connectome (the README says where: eight CSV.gz exports from
# codex.flywire.ai, CC-BY, free account, into data/female/) and run:
#   py build_graph_female.py
# The male build (build_graph.py, ~1.1 GB from a public bucket) still works.

numpy==2.4.2
scipy==1.17.1
pandas==3.0.1
pyarrow==23.0.1          # pandas needs it to read the .feather annotations
requests==2.32.5
Pillow==12.2.0           # decodes the screenshots the retina samples

fastapi==0.128.0
uvicorn[standard]==0.40.0

playwright==1.62.0       # the real browser the fly drives

eth-account==0.13.7      # signs on Robinhood Chain; never sends a key to a page
eth-utils>=5.3,<7        # 5.4.0 does not exist on PyPI (last 5.x is 5.3.1)
